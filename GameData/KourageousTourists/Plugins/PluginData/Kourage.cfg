KOURAGE
{
	noSkyDiving = false				// Prevents tourists from skydiving
	forceTouristsInSandbox = true	// Allow tourists to exist on Sandbox games

	paraglidingChutePitch = 0.6		// Paragliding pitch control, magic number; going too slow result is stalling, going too fast could kill the kerbal
	paraglidingDeployDelay = 5		// Delay before auto-deployng the parchute, sec
	paraglidingMaxAirpseed = 100	// Max airspeed for jumping out, m/s
	paraglidingMinAltAGL = 1500		// Min altitude AGL for jumping out, m
}
